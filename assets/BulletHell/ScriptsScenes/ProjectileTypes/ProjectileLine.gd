extends Line2D


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass


# Initialize with planned points,
# Thin instantly along the points,
# New line that moves along the points with thickness and texcture
# Hitbox that matches the thick line
# When thick line reaches end, delete

# Add a ray mode so it's easier to make it go wilding
